{
  "name": "llm-orchestrator",
  "version": "0.1.0",
  "description": "Routes chat/embed/image requests to local Ollama (RTX 5070) or future remote Mac (MLX) over Tailscale. OpenAI-compatible API in, OpenAI-compatible calls out.",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.21.1"
  }
}
